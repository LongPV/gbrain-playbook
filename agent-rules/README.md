# agent-rules

Rules that bind agents working in this repository — hard prohibitions, each
with the reasoning behind it and what to do instead. Linked from `AGENTS.md`
and `CLAUDE.md` so they are read before acting. One rule per file.
